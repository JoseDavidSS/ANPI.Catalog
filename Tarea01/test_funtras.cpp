#include "funtras.h"

using namespace std;

int main(){
    double base = ( sin_t(3 * divt(7)) + ln_t(2));
    double result =  (divt(cos_t(sqrt_t(2))) * (root_t(base, 3))) + log_t(pi(), exp_t(-1));
    cout<< "result: " << result << endl;
    return result;
}